<Window x:Class="KarateGeek.guis.NewCity"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New City" 
        Height="auto" 
        Width="auto"
        ResizeMode="NoResize">
    <Grid>
       
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            
            <Label Content="City name" 
                   Grid.Column="1"
                   Grid.Row="0"
                   VerticalAlignment="Center"
                   Margin="0,5"/>
            <TextBox Width="200"
                     Grid.Column="2"
                     VerticalAlignment="Center"
                     Margin="0,5"/>

            <Button Name="btnNewCity"
                Grid.Row="1"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                Margin="0,10"
                Content="Save new City"/>
        

    </Grid>
</Window>
