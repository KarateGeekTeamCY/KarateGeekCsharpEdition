<Window x:Class="KarateGeek.guis.ChooseKataWinner"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ChooseKataWinner" Height="173" Width="567">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Content="please choose the athlete that will pass to the next round" 
               Grid.Column="1" 
               Grid.Row="0" 
               HorizontalContentAlignment="Center"
               VerticalAlignment="Center"/>
        
        <ComboBox Name="tieList" 
                  Grid.Column="1" 
                  Grid.Row="1" 
                  VerticalAlignment="Center"/>
        
        <Button Name="saveWinner" 
                Grid.Column="1"
                Grid.Row="2" 
                Content="save" 
                VerticalAlignment="Center" Click="saveWinner_Click" />
        
    </Grid>
</Window>
